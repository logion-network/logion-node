version: "3.2"

services:
  dev:
    container_name: logion-node-prototype
    image: paritytech/ci-linux:974ba3ac-20201006
    working_dir: /var/www/logion-node
    ports:
      - "9944:9944"
    environment:
      - CARGO_HOME=/var/www/logion-node/.cargo
    volumes:
      - .:/var/www/logion-node
      - type: bind
        source: ./.local
        target: /root/.local
    command: bash -c "cargo build --release && ./target/release/logion-node --dev --ws-external"
